/**
 * 💀 ERADICATED BY SCR-988: Master Structural Fix.
 * Status: DEAD / STERILIZED.
 * Reason: Duplicate config at sub-path was causing build schisms.
 * Root directory next.config.ts is the ONLY source of truth.
 */
export {};
