
# وثيقة الهندسة الفنية: شجرة غرفة عمليات الناقل (الأوامر SC-042 -> SC-049)

هذا المستند هو المرجع السيادي الرسمي للبنية التقنية النهائية لواجهة الناقل الرئيسية، والتي تم تحويلها من "لوحة قيادة" إلى "غرفة عمليات" (Action Center).

---

### 1. شجرة الرسم التقني (Technical Architecture Tree)

هذا الرسم يوضح كيف تم تفكيك لوحة القيادة القديمة وإعادة بنائها كمركز عمليات حيوي.

```
/carrier (الدخول إلى واجهة الناقل)
│
├── layout.tsx (الهيكل الحاكم)
│   │
│   ├── Desktop Sidebar (قائمة الأوامر الكاملة لسطح المكتب)
│   │   ├── ... (لوحة القيادة، السوق، الصندوق، الرحلات)
│   │   └── ... (الأرشيف، الشروط الدائمة) -> [روابط إدارية]
│   │
│   └── Mobile Bottom Nav (شريط العمليات القتالية للموبايل)
│       └── ... (4 روابط رئيسية فقط) -> [تطبيق صارم لبروتوكول 13]
│
└── page.tsx (غرفة العمليات - الهدف الرئيسي للأمر SC-047)
    │
    ├── 🗑️ **[تم الاستئصال]** بطاقات الإحصاءات الجامدة (Revenue, Total Trips).
    │
    ├── 🧠 **العقل المدبر للصفحة (Page Logic):**
    │   ├── 1. **استعلام الحجوزات المعلقة:**
    │   │   └── query(..., where('status', '==', 'Pending-Carrier-Confirmation'), limit(3))
    │   │
    │   └── 2. **استعلام الرحلة القادمة:**
    │       └── query(..., where('status', '==', 'Planned'), orderBy('departureDate'), limit(1))
    │
    ├── 💉 **الحقنة الأولى: قسم "المهام العاجلة"**
    │   │   // يعرض أهم 3 طلبات تنتظر القرار
    │   ├── <BookingActionCard />
    │   ├── <BookingActionCard />
    │   └── <BookingActionCard />
    │
    └── 💉 **الحقنة الثانية: قسم "العملية القادمة"**
        │   // يعرض تفاصيل الرحلة التالية مباشرة
        └── <MyTripsList trips={[nextTrip]} />

```

---

### 2. التحليل الشمولي (Holistic Analysis)

#### 2.1. التحول من "لوحة عرض" إلى "غرفة عمليات"

بموجب **الأمر SC-047**، تم التخلي تماماً عن فكرة "لوحة القيادة" التقليدية التي تعرض أرقاماً ورسوماً بيانية لا قيمة لها في الميدان. تم استبدالها بـ "غرفة عمليات" تضع أمام الناقل أهم إجراءين يحتاجهما فور فتح التطبيق:

1.  **القرارات الفورية:** عرض طلبات الحجز المعلقة التي تحتاج إلى "قبول" أو "رفض".
2.  **الاستعداد للتحرك:** عرض تفاصيل الرحلة التالية مباشرة مع أدوات إدارتها.

**الأثر الاستراتيجي:** الناقل لم يعد بحاجة للتنقل بين الصفحات لتنفيذ 80% من مهامه اليومية. الشاشة الأولى أصبحت هي ساحة العمل.

#### 2.2. الامتثال للبروتوكولات السيادية

*   **بروتوكول 88 (حماية الموارد):**
    *   **استعلامات محدودة:** تم استخدام `limit(3)` لجلب الحجوزات المعلقة و `limit(1)` للرحلة القادمة. هذا يمنع منعاً باتاً استدعاء قوائم بيانات ضخمة ويحافظ على سرعة تحميل الصفحة واستهلاك الموارد.
    *   **إعادة استخدام المكونات:** تم "حقن" مكونات `BookingActionCard` و `MyTripsList` الموجودة مسبقاً، بدلاً من بناء مكونات جديدة، مما يمنع تكرار الكود.

*   **بروتوكول 13 (PWA & Visuals):**
    *   **التركيز البصري:** الواجهة الجديدة خالية من أي عناصر مشتتة. التركيز منصب بالكامل على "بطاقات الإجراءات" (Action Cards).
    *   **تصميم متجاوب:** إخفاء زر "تأسيس رحلة" من رأس الصفحة على الموبايل (`hidden md:flex`) يمنع الازدواجية مع الزر العائم (FAB)، مما يجعل الواجهة نظيفة ومصممة خصيصاً للهاتف.

*   **بروتوكول 16 (التطهير):**
    *   بموجب **الأمر SC-050**، تم تطهير جميع الملفات المعنية من أي تعليقات توجيهية أو أكواد ميتة، مما يجعلها جاهزة للأرشفة في المنطقة الحمراء كملفات "مختومة".

---

هذه الشجرة الهندسية توثق التحول الجذري في فلسفة تصميم واجهة الناقل، وتحصّن المكتسبات التي تم تحقيقها في سلسلة الأوامر من 42 إلى 49.
